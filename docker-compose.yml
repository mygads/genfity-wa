services:
  genfity-wa:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${GENFITY_PORT:-8080}:8080"
    environment:
      - GENFITY_ADMIN_TOKEN=${GENFITY_ADMIN_TOKEN}
      - GENFITY_GLOBAL_ENCRYPTION_KEY=${GENFITY_GLOBAL_ENCRYPTION_KEY}
      - GENFITY_GLOBAL_HMAC_KEY=${GENFITY_GLOBAL_HMAC_KEY:-}
      - GENFITY_GLOBAL_WEBHOOK=${GENFITY_GLOBAL_WEBHOOK:-}
      - DB_USER=${DB_USER:-genfity}
      - DB_PASSWORD=${DB_PASSWORD:-genfity}
      - DB_NAME=${DB_NAME:-genfity}
      - DB_HOST=db
      - DB_PORT=${DB_PORT:-5432}
      - TZ=${TZ:-America/Sao_Paulo}
      - WEBHOOK_FORMAT=${WEBHOOK_FORMAT:-json}
      - SESSION_DEVICE_NAME=${SESSION_DEVICE_NAME:-Genfity}
      # RabbitMQ configuration Optional
      - RABBITMQ_URL=amqp://genfity:genfity@rabbitmq:5672/
      - RABBITMQ_QUEUE=whatsapp_events
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - genfity-network
    restart: on-failure

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${DB_USER:-genfity}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-genfity}
      POSTGRES_DB: ${DB_NAME:-genfity}
    # ports:
    #   - "${DB_PORT:-5432}:5432" # Uncomment to access the database directly from your host machine.
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - genfity-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-genfity}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  rabbitmq:
    image: rabbitmq:3-management
    hostname: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: genfity
      RABBITMQ_DEFAULT_PASS: genfity
      RABBITMQ_DEFAULT_VHOST: /
    ports:
      - "5672:5672" # AMQP port
      - "15672:15672" # Management UI port
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - genfity-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

networks:
  genfity-network:
    driver: bridge

volumes:
  db_data:
  rabbitmq_data:
